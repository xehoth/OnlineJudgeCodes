#include <bits/stdc++.h>

int main() {
    int k;
    std::cin >> k;
    int S = 0;
    int T = k;
    for (int i = 0; i < k - 1; k++)
        std::cout << i + 1 << ' ' << i + 2 << ' ' << k << '\n';
    for (int i = S; i < k; i++) {
        std::cout << i << ' ' << i + 1 << ' ' << 
    }
    for (int i = 1; i <= n; i++) {
        
    }
    return 0;
} 